namespaces:

# Repeat the following data for each namespace

# For existing term lists, MUST be namespace assigned by issuing organization. For TDWG
# term lists, MUST follow conventional TDWG IRI patterns.
- namespace_uri: https://rs.tdwg.org/bdqdim/terms/

  # Standard namespace abbreviation for the namespace IRI.
  pref_namespace_prefix: bdqdim

  # Database name for associated directories and files in the rs.tdwg.org repository. 
  # MUST NOT contain spaces. SHOULD be descriptive and lowerCamelCase is RECOMMENDED. 
  # Borrowed term lists SHOULD use naming convention of Darwin and Audiovisual Cores.
  # Do not append -versions to this name, the versions directory will be created automatically.
  database: bdqdim

  # MUST be set to true if namespace not issued by TDWG in the rs.tdwg.org subdomain. 
  # MUST be set to false if namespace issued and controlled by TDWG.
  borrowed: false

  # Set to true if a new term list that has never been processed before. Otherwise, set to false.
  # MUST be set to true if it is a new term list that has never been processed before. 
  # Note that there are extra configuration files that must be set up for term lists that are 
  # part of new vocabularies. See Section 2.1.2 for details. 
  # MUST be set to false if this is an existing term list that has been processed at some time in the past.
  new_term_list: true

  # Normally set to false except for non-versioned namespaces like decisions.
  utility_namespace: false

  # Path to hand-edited changes CSV file. Relative to process directory from which the
  # process.py script is run.
  modifications_file_path: 

  # For TDWG-minted terms, SHOULD be set to empty string (Termlist IRI will be set to be
  # the same as the namespace IRI). For borrowed terms, mint an IRI that conforms to the 
  # TDWG termlist IRI pattern.

  # For TDWG-minted terms, this value SHOULD be the empty string and the termlist IRI will be set to be 
  # the same as the namespace IRI. If a value is given for TDWG-minted terms, it MUST be the same as the 
  # namespace IRI. When terms are borrowed from other non-TDWG vocabularies to be included within a TDWG 
  # vocabulary, an IRI for the borrowed term list conforming to the term list IRI pattern 
  # (https://github.com/tdwg/rs.tdwg.org#3rd-level-iris-denoting-term-lists) MUST be minted. 
  # The subdomain MUST be `rs.tdwg.org` and the first level IRI component following the subdomain MUST be 
  # the standard component for the vocabulary that is borrowing the terms. The second level IRI component 
  # SHOULD be a short, memorable string commonly associated with the borrowed vocabulary. Examples:
  # https://rs.tdwg.org/ac/xmp/ for the XMP terms borrowed by the Audiovisual Core
  # https://rs.tdwg.org/dwc/dcterms/ for the Dublin Core dcterms: terms borrowed by the Darwin Core
  termlist_uri: ''

  # Label used for the term list in machine-readable metadata.
  label: Data Quality Dimension Controlled Vocabulary Terms

  # Description of the term list used in machine-readable metadata.
  description: This list includes Named Individual terms defined in the BDQ data quality dimension namespace.

  # The following values are used to set up redirects to the list of terms document.

  # IRI string from List of Terms document URL to be prepended to the term fragment identifier when 
  # dereferencing terms and an HTML representation is requested. 
  prepend_url: https://bdq.tdwg.org/bdqdim/terms/

  # Indicates whether the namespace abbreviation is included in the fragment identifier for the term.
  use_namespace_in_fragment: false

  # String that us used to separate the namespace abbreviation from the term name in the fragment identifier.
  # If use_nameapace_in_fragment is false, this value is ignored.
  separator: '_'  

- namespace_uri: https://rs.tdwg.org/bdq/terms/

  # Standard namespace abbreviation for the namespace IRI.
  pref_namespace_prefix: bdq

  # Database name for associated directories and files in the rs.tdwg.org repository. 
  # MUST NOT contain spaces. SHOULD be descriptive and lowerCamelCase is RECOMMENDED. 
  # Borrowed term lists SHOULD use naming convention of Darwin and Audiovisual Cores.
  # Do not append -versions to this name, the versions directory will be created automatically.
  database: bdq

  # MUST be set to true if namespace not issued by TDWG in the rs.tdwg.org subdomain. 
  # MUST be set to false if namespace issued and controlled by TDWG.
  borrowed: false

  # Set to true if a new term list that has never been processed before. Otherwise, set to false.
  # MUST be set to true if it is a new term list that has never been processed before. 
  # Note that there are extra configuration files that must be set up for term lists that are 
  # part of new vocabularies. See Section 2.1.2 for details. 
  # MUST be set to false if this is an existing term list that has been processed at some time in the past.
  new_term_list: true

  # Normally set to false except for non-versioned namespaces like decisions.
  utility_namespace: false

  # Path to hand-edited changes CSV file. Relative to process directory from which the
  # process.py script is run.
  modifications_file_path: 

  # For TDWG-minted terms, SHOULD be set to empty string (Termlist IRI will be set to be
  # the same as the namespace IRI). For borrowed terms, mint an IRI that conforms to the 
  # TDWG termlist IRI pattern.

  # For TDWG-minted terms, this value SHOULD be the empty string and the termlist IRI will be set to be 
  # the same as the namespace IRI. If a value is given for TDWG-minted terms, it MUST be the same as the 
  # namespace IRI. When terms are borrowed from other non-TDWG vocabularies to be included within a TDWG 
  # vocabulary, an IRI for the borrowed term list conforming to the term list IRI pattern 
  # (https://github.com/tdwg/rs.tdwg.org#3rd-level-iris-denoting-term-lists) MUST be minted. 
  # The subdomain MUST be `rs.tdwg.org` and the first level IRI component following the subdomain MUST be 
  # the standard component for the vocabulary that is borrowing the terms. The second level IRI component 
  # SHOULD be a short, memorable string commonly associated with the borrowed vocabulary. Examples:
  # https://rs.tdwg.org/ac/xmp/ for the XMP terms borrowed by the Audiovisual Core
  # https://rs.tdwg.org/dwc/dcterms/ for the Dublin Core dcterms: terms borrowed by the Darwin Core
  termlist_uri: ''

  # Label used for the term list in machine-readable metadata.
  label: BDQ Controlled Vocabulary Terms

  # Description of the term list used in machine-readable metadata.
  description: This list includes terms defined in the BDQ name space for parameters and use cases.

  # The following values are used to set up redirects to the list of terms document.

  # IRI string from List of Terms document URL to be prepended to the term fragment identifier when 
  # dereferencing terms and an HTML representation is requested. 
  prepend_url: https://bdq.tdwg.org/bdq/terms/

  # Indicates whether the namespace abbreviation is included in the fragment identifier for the term.
  use_namespace_in_fragment: false

  # String that us used to separate the namespace abbreviation from the term name in the fragment identifier.
  # If use_nameapace_in_fragment is false, this value is ignored.
  separator: '_'  

- namespace_uri: https://rs.tdwg.org/bdqenh/terms/

  # Standard namespace abbreviation for the namespace IRI.
  pref_namespace_prefix: bdqenh

  # Database name for associated directories and files in the rs.tdwg.org repository. 
  # MUST NOT contain spaces. SHOULD be descriptive and lowerCamelCase is RECOMMENDED. 
  # Borrowed term lists SHOULD use naming convention of Darwin and Audiovisual Cores.
  # Do not append -versions to this name, the versions directory will be created automatically.
  database: bdqenh

  # MUST be set to true if namespace not issued by TDWG in the rs.tdwg.org subdomain. 
  # MUST be set to false if namespace issued and controlled by TDWG.
  borrowed: false

  # Set to true if a new term list that has never been processed before. Otherwise, set to false.
  # MUST be set to true if it is a new term list that has never been processed before. 
  # Note that there are extra configuration files that must be set up for term lists that are 
  # part of new vocabularies. See Section 2.1.2 for details. 
  # MUST be set to false if this is an existing term list that has been processed at some time in the past.
  new_term_list: true

  # Normally set to false except for non-versioned namespaces like decisions.
  utility_namespace: false

  # Path to hand-edited changes CSV file. Relative to process directory from which the
  # process.py script is run.
  modifications_file_path: 

  # For TDWG-minted terms, SHOULD be set to empty string (Termlist IRI will be set to be
  # the same as the namespace IRI). For borrowed terms, mint an IRI that conforms to the 
  # TDWG termlist IRI pattern.

  # For TDWG-minted terms, this value SHOULD be the empty string and the termlist IRI will be set to be 
  # the same as the namespace IRI. If a value is given for TDWG-minted terms, it MUST be the same as the 
  # namespace IRI. When terms are borrowed from other non-TDWG vocabularies to be included within a TDWG 
  # vocabulary, an IRI for the borrowed term list conforming to the term list IRI pattern 
  # (https://github.com/tdwg/rs.tdwg.org#3rd-level-iris-denoting-term-lists) MUST be minted. 
  # The subdomain MUST be `rs.tdwg.org` and the first level IRI component following the subdomain MUST be 
  # the standard component for the vocabulary that is borrowing the terms. The second level IRI component 
  # SHOULD be a short, memorable string commonly associated with the borrowed vocabulary. Examples:
  # https://rs.tdwg.org/ac/xmp/ for the XMP terms borrowed by the Audiovisual Core
  # https://rs.tdwg.org/dwc/dcterms/ for the Dublin Core dcterms: terms borrowed by the Darwin Core
  termlist_uri: ''

  # Label used for the term list in machine-readable metadata.
  label: Data Quality Enhancement Controlled Vocabulary Terms

  # Description of the term list used in machine-readable metadata.
  description: Named Individuals of the bdqffdq:Enhancement class provided as a controlled vocabulary in the BDQ standard.

  # The following values are used to set up redirects to the list of terms document.

  # IRI string from List of Terms document URL to be prepended to the term fragment identifier when 
  # dereferencing terms and an HTML representation is requested. 
  prepend_url: https://bdq.tdwg.org/bdqenh/terms/

  # Indicates whether the namespace abbreviation is included in the fragment identifier for the term.
  use_namespace_in_fragment: false

  # String that us used to separate the namespace abbreviation from the term name in the fragment identifier.
  # If use_nameapace_in_fragment is false, this value is ignored.
  separator: '_'  


- namespace_uri: https://rs.tdwg.org/bdqcrit/terms/

  # Standard namespace abbreviation for the namespace IRI.
  pref_namespace_prefix: bdqcrit

  # Database name for associated directories and files in the rs.tdwg.org repository. 
  # MUST NOT contain spaces. SHOULD be descriptive and lowerCamelCase is RECOMMENDED. 
  # Borrowed term lists SHOULD use naming convention of Darwin and Audiovisual Cores.
  # Do not append -versions to this name, the versions directory will be created automatically.
  database: bdqcrit

  # MUST be set to true if namespace not issued by TDWG in the rs.tdwg.org subdomain. 
  # MUST be set to false if namespace issued and controlled by TDWG.
  borrowed: false

  # Set to true if a new term list that has never been processed before. Otherwise, set to false.
  # MUST be set to true if it is a new term list that has never been processed before. 
  # Note that there are extra configuration files that must be set up for term lists that are 
  # part of new vocabularies. See Section 2.1.2 for details. 
  # MUST be set to false if this is an existing term list that has been processed at some time in the past.
  new_term_list: true

  # Normally set to false except for non-versioned namespaces like decisions.
  utility_namespace: false

  # Path to hand-edited changes CSV file. Relative to process directory from which the
  # process.py script is run.
  modifications_file_path: 

  # For TDWG-minted terms, SHOULD be set to empty string (Termlist IRI will be set to be
  # the same as the namespace IRI). For borrowed terms, mint an IRI that conforms to the 
  # TDWG termlist IRI pattern.

  # For TDWG-minted terms, this value SHOULD be the empty string and the termlist IRI will be set to be 
  # the same as the namespace IRI. If a value is given for TDWG-minted terms, it MUST be the same as the 
  # namespace IRI. When terms are borrowed from other non-TDWG vocabularies to be included within a TDWG 
  # vocabulary, an IRI for the borrowed term list conforming to the term list IRI pattern 
  # (https://github.com/tdwg/rs.tdwg.org#3rd-level-iris-denoting-term-lists) MUST be minted. 
  # The subdomain MUST be `rs.tdwg.org` and the first level IRI component following the subdomain MUST be 
  # the standard component for the vocabulary that is borrowing the terms. The second level IRI component 
  # SHOULD be a short, memorable string commonly associated with the borrowed vocabulary. Examples:
  # https://rs.tdwg.org/ac/xmp/ for the XMP terms borrowed by the Audiovisual Core
  # https://rs.tdwg.org/dwc/dcterms/ for the Dublin Core dcterms: terms borrowed by the Darwin Core
  termlist_uri: ''

  # Label used for the term list in machine-readable metadata.
  label: Data Quality Criterion Controlled Vocabulary Terms

  # Description of the term list used in machine-readable metadata.
  description: Named Individuals of the bdqffdq:Criterion class provided as a controlled vocabulary in the BDQ standard.

  # The following values are used to set up redirects to the list of terms document.

  # IRI string from List of Terms document URL to be prepended to the term fragment identifier when 
  # dereferencing terms and an HTML representation is requested. 
  prepend_url: https://bdq.tdwg.org/bdqcrit/terms/

  # Indicates whether the namespace abbreviation is included in the fragment identifier for the term.
  use_namespace_in_fragment: false

  # String that us used to separate the namespace abbreviation from the term name in the fragment identifier.
  # If use_nameapace_in_fragment is false, this value is ignored.
  separator: '_'  


- namespace_uri: https://rs.tdwg.org/bdqtest/terms/

  # Standard namespace abbreviation for the namespace IRI.
  pref_namespace_prefix: bdqtest

  # Database name for associated directories and files in the rs.tdwg.org repository. 
  # MUST NOT contain spaces. SHOULD be descriptive and lowerCamelCase is RECOMMENDED. 
  # Borrowed term lists SHOULD use naming convention of Darwin and Audiovisual Cores.
  # Do not append -versions to this name, the versions directory will be created automatically.
  database: bdqtest

  # MUST be set to true if namespace not issued by TDWG in the rs.tdwg.org subdomain. 
  # MUST be set to false if namespace issued and controlled by TDWG.
  borrowed: false

  # Set to true if a new term list that has never been processed before. Otherwise, set to false.
  # MUST be set to true if it is a new term list that has never been processed before. 
  # Note that there are extra configuration files that must be set up for term lists that are 
  # part of new vocabularies. See Section 2.1.2 for details. 
  # MUST be set to false if this is an existing term list that has been processed at some time in the past.
  new_term_list: true

  # Normally set to false except for non-versioned namespaces like decisions.
  utility_namespace: false

  # Path to hand-edited changes CSV file. Relative to process directory from which the
  # process.py script is run.
  modifications_file_path: 

  # For TDWG-minted terms, SHOULD be set to empty string (Termlist IRI will be set to be
  # the same as the namespace IRI). For borrowed terms, mint an IRI that conforms to the 
  # TDWG termlist IRI pattern.

  # For TDWG-minted terms, this value SHOULD be the empty string and the termlist IRI will be set to be 
  # the same as the namespace IRI. If a value is given for TDWG-minted terms, it MUST be the same as the 
  # namespace IRI. When terms are borrowed from other non-TDWG vocabularies to be included within a TDWG 
  # vocabulary, an IRI for the borrowed term list conforming to the term list IRI pattern 
  # (https://github.com/tdwg/rs.tdwg.org#3rd-level-iris-denoting-term-lists) MUST be minted. 
  # The subdomain MUST be `rs.tdwg.org` and the first level IRI component following the subdomain MUST be 
  # the standard component for the vocabulary that is borrowing the terms. The second level IRI component 
  # SHOULD be a short, memorable string commonly associated with the borrowed vocabulary. Examples:
  # https://rs.tdwg.org/ac/xmp/ for the XMP terms borrowed by the Audiovisual Core
  # https://rs.tdwg.org/dwc/dcterms/ for the Dublin Core dcterms: terms borrowed by the Darwin Core
  termlist_uri: ''

  # Label used for the term list in machine-readable metadata.
  label: BDQ Tests and Assertions

  # Description of the term list used in machine-readable metadata.
  description: This list includes the BDQ Test definitions.

  # The following values are used to set up redirects to the list of terms document.

  # IRI string from List of Terms document URL to be prepended to the term fragment identifier when 
  # dereferencing terms and an HTML representation is requested. 
  prepend_url: https://bdq.tdwg.org/bdqtest/terms/

  # Indicates whether the namespace abbreviation is included in the fragment identifier for the term.
  use_namespace_in_fragment: false

  # String that us used to separate the namespace abbreviation from the term name in the fragment identifier.
  # If use_nameapace_in_fragment is false, this value is ignored.
  separator: '_'  
